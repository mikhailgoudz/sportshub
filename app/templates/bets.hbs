<div class="container">
<div class="mt-5">
  <div class="container text-center">
    <h2>Bet Slips</h2>
  </div>
</div>

<div class="container">
<div class="row">
{{#each model as |bet|}}
<div class="col-4">
  <div class="card mt-5 mb-5" style="width: 20rem;">
      <h3 class="text-center">{{bet.sport}}</h3>
      <div class="card-body">
               
        <h4> Team: {{bet.team}} <img src={{bet.teamLogo}} alt="" height = "30" width="40"> </h4>
        <h4>Odds: {{#if bet.positiveOdds}} +{{/if}}{{bet.odds}}</h4>
        <h5>Wager: ${{bet.wager}}</h5>
        <h5>Potential Payout: ${{bet.payout}}</h5>
         <p>{{bet.datePlaced}}</p>
      </div>
    </div>
    </div>

{{/each}}
</div>
</div>

</div>
{{outlet}}